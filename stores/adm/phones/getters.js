const getters = {

    // selectPlan(state) {
    //     if (state.idSelectedPlan) {
    //         state.selected = state.data.find(el => el.id === state.idSelectedPlan)
    //     }
    // },

};

export default { ...getters };
  